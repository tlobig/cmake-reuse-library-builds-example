#include <iostream>

namespace interface_lib
{
    class interface_lib_class
    {
        public:
        void call() { std::cout << " Happy to inform you about a liberal interface library call. Cheers." << std::endl; }

    };
}